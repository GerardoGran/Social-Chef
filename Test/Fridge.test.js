import * as React fromm 'react';
import {Text, TouchableOpacity} from 'react-native';
import Fridge from '../Views/Fridge'
improt render from 'react-test-renderer';


function Button({styles, children}){
    retunr (
        
    )
}