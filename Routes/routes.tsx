import React from 'react';
import { createStacNavigation } from '@react-navigation/stack';
import { NavigationContainer } from '@react-navigation/native';

interface routesProps {

}

export const routes: React.FC<routesProps> = ({}) => {
        return ();
}